# 範例環境變數檔。複製成 .env 後填入你的值(.env 已被 .gitignore 排除,不會進版控)。
#
# FinMind API token(選填):
#   - 不填 → 走匿名模式,有較低的每小時請求上限(本機少量股票夠用)。
#   - 想提高額度(尤其 GitHub Actions 每日掃多檔)→ 到 https://finmindtrade.com 免費註冊,
#     取得 api token 後填在這裡;CI 上則設成 GitHub Secret「FINMIND_TOKEN」。
FINMIND_TOKEN=
