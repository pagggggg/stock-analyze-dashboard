"""
參數集中管理 (params.py)
========================
過擬合防護的核心:**只有 2 個可調訊號參數**(遠低於題目要求的「≤3 個」)。
持有期(3/6/12 月)是「輸出維度」不是拿來最佳化的參數——三個期間全部照實回報,
不挑對自己有利的那個。其餘都是「固定結構條件」或「資料範圍設定」,不參與調參,
所以不算進過擬合預算。

改這裡的數字就能重跑不同設定;報告會把用到的參數逐條印出來。
"""

from __future__ import annotations

# ─────────────────────────────────────────────────────────────────────
# ★ 可調訊號參數(過擬合預算:2 個,上限 3 個)★
# ─────────────────────────────────────────────────────────────────────
# 代理訊號一:月營收 YoY「突然加速」的門檻(百分點)。
#   加速度 = 本月 YoY − 上月 YoY。>= 這個值就算「營收動能上修」。
REV_ACCEL_PP = 20.0          # 參數①(題目指定預設 20 個百分點)

# 代理訊號二:單季 EPS YoY 的門檻(%)。
#   EPS_YoY = 本季EPS / 去年同季EPS − 1。>= 這個值 **且** 超越前四季 YoY 趨勢,
#   才算「盈餘大幅上修」。
EPS_YOY_PCT = 50.0           # 參數②(題目指定預設 50%)

# ── 就這兩個。以下都不是「可調訊號參數」,不參與最佳化。 ──────────────────

# ─────────────────────────────────────────────────────────────────────
# 固定結構條件(寫死,不調;純粹是把訊號定義講清楚,不是拿來 fit 的旋鈕)
# ─────────────────────────────────────────────────────────────────────
# 代理一:除了加速,還要求「本月 YoY > 0」(真的在成長,不是衰退變比較不衰退)。
# 代理二:「超前四季趨勢」= 本季 YoY > 前四季 YoY 的平均(成長在加速而非趨緩)。
#         且去年同季 EPS 必須 > 0(否則 YoY 沒有意義)。
# 這些是訊號「定義的一部分」,固定不動。

# ─────────────────────────────────────────────────────────────────────
# 持有期(輸出維度,不是可調參數):三個都報,不挑好看的
# ─────────────────────────────────────────────────────────────────────
HOLDING_MONTHS = [3, 6, 12]

# ─────────────────────────────────────────────────────────────────────
# 資料範圍 / 抽樣設定(工程設定,非訊號調參)
# ─────────────────────────────────────────────────────────────────────
BENCHMARK = "0050"           # 超額報酬對照基準(元大台灣50)

UNIVERSE_SEED = 42           # 隨機抽樣種子(可重現);對照組亦用同一池
UNIVERSE_SIZE = 600          # 從全體普通股(約 2100 檔)隨機抽樣的檔數(代表性樣本)

# 研究時窗:訊號觸發日必須落在此區間(確保 12 個月持有期都有完整未來報酬)
STUDY_START = "2020-07-01"   # 最早觸發日
STUDY_END = "2025-06-30"     # 最晚觸發日(留 >12 個月給未來報酬跑完)

# 抓取起點(要比研究時窗更早,才有 YoY / 前四季 的歷史可算)
REVENUE_FETCH_START = "2018-06-01"
EPS_FETCH_START = "2018-01-01"
PRICE_FETCH_START = "2020-01-01"

# 樣本內 / 樣本外分界(依觸發日切):之前=IS,之後=OOS
IS_OOS_SPLIT = "2023-07-01"

# 對照組:每個真實訊號配對幾個「隨機選股」(同觸發日、同持有期)
CONTROL_DRAWS_PER_SIGNAL = 3
CONTROL_SEED = 123
